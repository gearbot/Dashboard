import {Component} from "preact";
import {CommandGroupProps} from "../../utils/Interfaces";

export default class CommandGroup extends Component<CommandGroupProps, {}> {
    render() {
        return (
            <>
            </>
        )
    }
}