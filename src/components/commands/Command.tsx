import {Component} from "preact";
import {CommandProps} from "../../utils/Interfaces";

export default class Command extends Component<CommandProps, {}> {
    render() {
        return (
            <>
            </>
        )
    }
}