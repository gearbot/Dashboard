import {Component} from "preact";
export default class CheckmarkField extends Component<{}, {}> {

    constructor(props, state) {
        super(props, state);
    }

    render() {
        return (
            <div/>
        );
    }
}